% | SVG - Enables SVG support in LaTeX
% |
% | The svg package adds support for Scalable Vector Graphics (SVG) to LaTeX documents. 
% | It allows for SVG images to be included in a LaTeX document, converting them to PDF format on 
% | the fly.
% |
% | No parameters are used with this package declaration.
\usepackage{svg}