\usepackage{makecell}

% | tabulary - Package for creating tables with automatic column width 
% | calculation based on content.
% |
% | The tabulary package is particularly useful when dealing with tables that 
% | contain a lot of text content. 
% | It automatically adjusts the column widths to balance the table, based on 
% | the amount and type of content in each cell. 
% | It aims to make the table's total width equal to the text width while 
% | maintaining the readability and aesthetics of the cell content.
% |
% | Parameters:
% | This package does not require parameters during import. 
% | Usage details should be referred in the package's specific commands.
\usepackage{tabulary}
\usepackage{longtable}